"""Read-only dashboard over the swing-ranking-v1 forward ledger and backtests.

Nothing in this package writes to a run directory, and nothing here imports
`sts.swing_ranking.forward`. The scheduler remains the single writer.
"""
