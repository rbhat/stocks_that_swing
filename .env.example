# Copy to .env and fill in. Never commit .env.
# No environment variables are read by the Phase-1 modules; sts.env.load()
# is a generic loader kept for future phases. Nothing to configure yet.

# rclone remote name reaching the forward-paper Drive folders (verified
# working: `rclone lsjson --drive-root-folder-id 1DIk5ZC-pHq5BGShgjXIqZ_O1nZ636gi5 gdrive: --max-depth 1`)
STS_RCLONE_REMOTE=gdrive:
